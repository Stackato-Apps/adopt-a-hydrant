applications:
- name: adopt-a-hydrant
  instances: 1
  mem: 256M
  services:
    ${name}-db:
      type: postgresql
  stackato:
    hooks:
      post-staging:
      - bundle exec rake db:migrate
      - bundle exec rake db:seed
