name: hydrant
instances: 1
framework:
  type: rails3
  runtime: ruby19
mem: 256M
services:
  pg-hydrant: postgresql
# hooks:
#   pre-running:
#     - bundle exec rake threadsafe:disabled
#     - bundle exec rake db:migrate
#     - bundle exec rake db:schema:load
#     - bundle exec rake db:seed
#     - bundle exec rake threadsafe:enabled
    
